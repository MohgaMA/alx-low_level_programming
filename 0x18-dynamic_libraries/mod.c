#include "main.h"
/**
 * mod - function modulas two numbers
 * @a : is a parameter
 * @b : is a prameter
 * Return: the result of the them
 */
int mod(int a, int b)
{
	int result = a % b;

	return (result);
}
