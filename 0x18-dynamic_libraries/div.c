#include "main.h"
/**
 * div - function that division two numbers
 * @a: is a parameter
 * @b: is a prameter
 * Return: the result of the them
 */
int div(int a, int b)
{
	int result = a / b;

	return (result);
}
