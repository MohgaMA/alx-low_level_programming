#include "main.h"
/**
 * mul - function multiblicate two numbers
 * @a: is a parameter
 * @b: is a prameter
 * Return: the result of the them
 */
int mul(int a, int b)
{
	int result = a + b;

	return (result);
}
